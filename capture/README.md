# Capture

Raw-material inbox: upstream notes, release-note excerpts, profiling dumps, and
rough task notes. Distill the reusable part into `knowledge/`, `tools/`, or
`workflows/` before it becomes default agent context.

Start from `templates/capture-note.md`.
